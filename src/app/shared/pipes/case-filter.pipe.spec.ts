import { CaseFilterPipe } from './case-filter.pipe';

describe('CaseFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CaseFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
