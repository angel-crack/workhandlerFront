import { NormalizeCasesPipe } from './normalize-cases.pipe';

describe('NormalizeCasesPipe', () => {
  it('create an instance', () => {
    const pipe = new NormalizeCasesPipe();
    expect(pipe).toBeTruthy();
  });
});
